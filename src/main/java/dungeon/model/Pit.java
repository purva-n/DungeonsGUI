package dungeon.model;

public class Pit extends AbstractEntity{
  @Override
  public void updateTreasureQuantity(TreasureType stone, int quantity) {
    // empty because Pit does not contain treasure
  }
}
